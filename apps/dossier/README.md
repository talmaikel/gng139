# GnG building dossier

Static Cloudflare Workers site for the interactive building dossier currently published at `gng139.online`.

## Deploy

From this directory, run:

```sh
npx wrangler deploy
```

Cloudflare Builds should use this directory as the project root and `npx wrangler deploy` as the deploy command.
