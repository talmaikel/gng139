# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30

# --- DB / ORM / migrations ---
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
psycopg2-binary>=2.9  # sync driver used by Alembic migrations
geoalchemy2>=0.15
alembic>=1.13

# --- Auth / multi-tenancy ---
fastapi-users[sqlalchemy]>=13.0

# --- Settings / validation ---
pydantic>=2.7
pydantic-settings>=2.3

# --- Background queue (pure Postgres, no Redis/RabbitMQ) ---
procrastinate[psycopg]>=2.9

# --- Data pipeline: scraping / OCR / CV ---
playwright>=1.44
pytesseract>=0.3.13
opencv-python-headless>=4.10
pillow>=10.3
numpy>=1.26
pymupdf>=1.24  # rasterizes scraped PDF permit files into page images for OCR

# --- OpenAI fallback extraction (structured outputs) ---
openai>=1.35

# --- Utilities ---
python-dotenv>=1.0
httpx>=0.27
