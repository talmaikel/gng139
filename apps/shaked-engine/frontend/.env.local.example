# Local development only. Copy to .env.local and fill in the existing dev account.
# These variables are server-side only and are never exposed to browser JavaScript.
DEV_AUTO_LOGIN_EMAIL=
DEV_AUTO_LOGIN_PASSWORD=

# Optional: backend origin used by Next.js server-side requests and API proxying.
API_ORIGIN=http://127.0.0.1:8000
