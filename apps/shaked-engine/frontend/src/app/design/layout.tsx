import "@mantine/carousel/styles.css";

// Trial route for the Mantine design language. Theme and fonts come from the root layout.
export default function DesignLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
